#include <bits/stdc++.h>
using namespace std;
#define _GLIBCXX_DEBUG
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
    string s;
    cin >> s;
    // swap(s[0],s[1]);
    // cout << s << endl;
    for(auto c:s) {
        // cout << c << endl;
        if(c == 'B')
    }

    return 0;
}